<div class="container">
  <div class="logo" id='title'>{{#link-to "index"}}<img src="/images/logo.png" alt="My Hearth">{{/link-to}}</div>
  <div class="login menu">
    {{#if session.isAuthenticated}}
      <button {{ action 'invalidateSession' }} class="btn btn-default">Logout</button>
    {{else}}
      {{#link-to 'login' class="btn btn-default"}}Login{{/link-to}}
    {{/if}}
  </div>
  <div class="main container">
    {{#link-to 'index'}}Home{{/link-to}}
    {{#if session.isAuthenticated}}
      {{#link-to 'collection'}}Collection{{/link-to}}
      {{#link-to 'packs'}}Pack Info{{/link-to}}
      {{#link-to 'stats'}}Collection Stats{{/link-to}}
      {{#link-to 'deckslist'}}Decks{{/link-to}}
      {{#link-to 'scrape'}}Scrape{{/link-to}}
    {{/if}}
    <hr class="hr break">

    <div id="messages"></div>

    <div class="main-container">
      {{outlet}}
    </div>
  </div>
</div>
