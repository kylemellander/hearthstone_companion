import { cardsInDeckByType } from '../../../helpers/cards-in-deck-by-type';
import { module, test } from 'qunit';

module('Unit | Helper | cards in deck by type');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cardsInDeckByType(42);
  assert.ok(result);
});
