import { displayStats } from '../../../helpers/display-stats';
import { module, test } from 'qunit';

module('Unit | Helper | display stats');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = displayStats(42);
  assert.ok(result);
});
