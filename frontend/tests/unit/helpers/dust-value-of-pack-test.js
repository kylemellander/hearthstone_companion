import { dustValueOfPack } from '../../../helpers/dust-value-of-pack';
import { module, test } from 'qunit';

module('Unit | Helper | dust value of pack');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dustValueOfPack(42);
  assert.ok(result);
});
