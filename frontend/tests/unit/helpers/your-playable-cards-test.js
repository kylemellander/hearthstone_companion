import { yourPlayableCards } from '../../../helpers/your-playable-cards';
import { module, test } from 'qunit';

module('Unit | Helper | your playable cards');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = yourPlayableCards(42);
  assert.ok(result);
});
