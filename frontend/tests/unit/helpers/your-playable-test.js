import { yourPlayable } from '../../../helpers/your-playable';
import { module, test } from 'qunit';

module('Unit | Helper | your playable');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = yourPlayable(42);
  assert.ok(result);
});
