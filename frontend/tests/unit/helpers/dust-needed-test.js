import { dustNeeded } from '../../../helpers/dust-needed';
import { module, test } from 'qunit';

module('Unit | Helper | dust needed');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dustNeeded(42);
  assert.ok(result);
});
