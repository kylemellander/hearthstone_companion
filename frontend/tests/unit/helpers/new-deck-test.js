import { newDeck } from '../../../helpers/new-deck';
import { module, test } from 'qunit';

module('Unit | Helper | new deck');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = newDeck(42);
  assert.ok(result);
});
