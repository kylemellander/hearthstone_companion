import { totalPlayable } from '../../../helpers/total-playable';
import { module, test } from 'qunit';

module('Unit | Helper | total playable');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = totalPlayable(42);
  assert.ok(result);
});
